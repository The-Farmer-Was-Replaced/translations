@code_tooltip_harvest = `harvest()`
Cose el objeto debajo del dron.
Si cosechas un objeto que no puede ser cosechado, será destruido.

devuelve `True` si un objeto fue removido, `False` de lo contrario.

toma `200` ticks para ejecutarse si un objeto fue removido, `1` tick de lo contrario.

ejemplo de uso:
`harvest()`
@code_tooltip_can_harvest = `can_harvest()`
Se usa para saber si las plantas están completamente crecidas.

devuelve `True` si hay un objeto listo para ser cosechado debajo del dron, `False` de lo contrario.

toma `1` tick para ejecutarse.

ejemplo de uso:
`if can_harvest():
    harvest()`
@code_tooltip_range = `range()`
Genera una secuencia de números.

sobrecargas:
`range(end)` devuelve una secuencia de números desde `0` (inclusive) hasta `end` (exclusivo).
`range(start,end)` devuelve una secuencia de números desde `start` (inclusive) hasta `end` (exclusivo).
`range(start,end,step)` devuelve una secuencia de números desde `start` (inclusive) hasta `end` (exclusivo) en pasos de tamaño `step`.

toma `1` tick para ejecutarse.

ejemplo de uso:
`for i in range(10):
    print(i)

for i in range(2,6):
    print(i)

for i in range(10, 0, -1):
    print(i)`
@code_tooltip_plant = `plant(entity)`
Gasta el costo del `entity` especificado y lo planta debajo del dron.
Falla si no puedes permitirte la planta, el tipo de suelo es incorrecto o ya hay una planta allí.

devuelve `True` si tuvo éxito, `False` de lo contrario.

toma `200` ticks para ejecutarse si tuvo éxito, `1` tick de lo contrario.

ejemplo de uso:
`plant(Entities.Bush)`
@code_tooltip_move = `move(direction)`
Mueve el dron en la `dirección` especificada un tile.
Si el dron se mueve sobre el borde de la granja, vuelve al otro lado de la granja.

`East `  =  derecha
`West `  =  izquierda
`North`  =  arriba
`South`  =  abajo

devuelve `True` si el dron se movió, `False` de lo contrario.

toma `200` ticks para ejecutarse si el dron se movió, `1` tick de lo contrario.

ejemplo de uso:
`move(North)`
@code_tooltip_swap = `swap(direction)`
Intercambia el objeto debajo del dron con el objeto vecino en la `dirección` especificada.
No funciona con todos los objetos.
También funciona si uno (o ambos) de los objetos son `None`.

devuelve `True` si tuvo éxito, `False` de lo contrario.

toma `200` ticks para ejecutarse en éxito, `1` tick de lo contrario.

ejemplo de uso:
`swap(North)`
@code_tooltip_till = `till()`
Prepara el suelo debajo del dron a `Grounds.Soil`. Si ya es suelo, cambiará el suelo de vuelta a `Grounds.Grassland`.

devuelve `None`

toma `200` ticks para ejecutarse.

ejemplo de uso:
`till()`
@code_tooltip_get_pos_x = `get_pos_x()`
Obtiene la posición x actual del dron.
La posición x comienza en `0` en el `West` y aumenta en la dirección `East`.

devuelve un número que representa la coordenada x actual del dron.

toma `1` tick para ejecutarse.

ejemplo de uso:
`x, y = get_pos_x(), get_pos_y()`
@code_tooltip_get_pos_y = `get_pos_y()`
Obtiene la posición y actual del dron.
La posición y comienza en `0` en el `South` y aumenta en la dirección `North`.

devuelve un número que representa la coordenada y actual del dron.

toma `1` tick para ejecutarse.

ejemplo de uso:
`x, y = get_pos_x(), get_pos_y()`
@code_tooltip_get_world_size = `get_world_size()`
Obtiene el tamaño actual de la granja.

devuelve la longitud lateral de la cuadrícula en dirección norte a sur.

toma `1` tick para ejecutarse.

ejemplo de uso:
`for i in range(get_world_size()):
    move(North)`
@code_tooltip_get_entity_type = `get_entity_type()`
Descubre qué tipo de entidad está debajo del dron.

devuelve `None` si el tile está vacío, de lo contrario devuelve el tipo de entidad debajo del dron.

toma `1` tick para ejecutarse.

ejemplo de uso:
`if get_entity_type() == Entities.Grass:
    harvest()`
@code_tooltip_get_ground_type = `get_ground_type()`
Descubre qué tipo de suelo está debajo del dron.

devuelve el tipo de suelo debajo del dron.

toma `1` tick para ejecutarse.

ejemplo de uso:
`if get_ground_type() != Grounds.Soil:
    till()`
@code_tooltip_get_time = `get_time()`
Obtiene el tiempo de juego actual.

devuelve el tiempo en segundos desde el inicio del juego.

toma `1` tick para ejecutarse.

ejemplo de uso:
`start = get_time()

do_something()

time_passed = get_time() - start`
@code_tooltip_get_tick_count = `get_tick_count()`
Usado para medir el número de ticks ejecutados.

devuelve el número de ticks ejecutados desde el inicio de la ejecución.

toma `0` ticks para ejecutarse.

ejemplo de uso:
`do_something()

print(get_tick_count())`
@code_tooltip_trade = `trade(item)`
Intenta comprar el `item` especificado.
Si el `item` no puede comprarse o no tienes los recursos necesarios, simplemente no hace nada.

sobrecargas:
`trade(item)`: Compra el `item` una vez.
`trade(item, n)`: Si `Unlocks.Multi_Trade` está desbloqueado, esto comprará el `item` `n` veces inmediatamente. Si no puedes permitirte todos los `n` items, no comprará ninguno. Si `Unlocks.Multi_Trade` no está desbloqueado, lanza un error.

devuelve `True` si pudo comprar los artículos, `False` de lo contrario.

toma `200` ticks para ejecutarse si tuvo éxito, `1` tick de lo contrario.

ejemplo de uso:
`if num_unlocked(Unlocks.Multi_Trade) > 0:
    trade(Items.Carrot_Seed, 10)
else:
    for i in range(10):
        trade(Items.Carrot_Seed)`
@code_tooltip_use_item = `use_item(item, n=1)`
Intenta usar el `item` especificado `n` veces. Solo puede usarse con algunos artículos incluyendo `Items.Water`, `Items.Fertilizer` y `Items.Egg`.

devuelve `True` si un artículo fue usado, `False` de lo contrario.

toma `200` ticks para ejecutarse si tuvo éxito, `1` tick de lo contrario.

ejemplo de uso:
`use_item(Items.Fertilizer)`
@code_tooltip_get_water = `get_water()`
Obtiene el nivel actual de agua debajo del dron.

devuelve el nivel de agua debajo del dron como un número entre `0` y `1`.

toma `1` tick para ejecutarse.

ejemplo de uso:
`if get_water() < 0.5:
    use_item(Items.Water)`
@code_tooltip_do_a_flip = `do_a_flip()`
Hace que el dron haga un giro! Esta acción no está afectada por las mejoras de velocidad.

devuelve `None`

toma `1s` para ejecutarse.

ejemplo de uso:
`while True:
    do_a_flip()`
@code_tooltip_print = `print(something)`
Imprime `something` en el aire sobre el dron usando humo. Esta acción no está afectada por las mejoras de velocidad.
Se pueden imprimir múltiples valores a la vez.

devuelve `None`

toma `1s` para ejecutarse.

ejemplo de uso:
`print("ground:", get_ground_type())`
@code_tooltip_len = `len(collection)`
Obtiene el número de elementos en una lista, conjunto, diccionario o tupla.

devuelve la longitud de la `collection`.

toma `1` tick para ejecutarse.

ejemplo de uso:
`for i in range(len(list)):
    list[i] += 1`
@code_tooltip_list = `list()`
Crea una nueva lista.

sobrecargas:
`list(collection)`: Crea una lista con los elementos de una lista, conjunto, diccionario o tupla existente.

devuelve una lista.

toma `1 + len(collection)` ticks para ejecutarse.

ejemplo de uso:
`new_list = list((1,2,3))`
@code_tooltip_dict = `dict()`
Crea un nuevo diccionario.

sobrecargas:
`dict(dictionary)`: Crea una copia de `dictionary`.

devuelve un diccionario.

toma `1 + len(dictionary)` ticks para ejecutarse.

ejemplo de uso:
`new_dict = dict()`
@code_tooltip_set = `set()`
Crea un nuevo conjunto.

sobrecargas:
`set(collection)`: Crea un conjunto con los elementos de una lista, conjunto, diccionario o tupla existente.

devuelve un conjunto.

toma `1 + len(collection)` ticks para ejecutarse.

ejemplo de uso:
`new_set = set((1,2,3))`
@code_tooltip_str = `str(object)`

devuelve una representación en cadena de `object`.

toma `1` tick para ejecutarse.

ejemplo de uso:
`string = str(1000)`
@code_tooltip_set_execution_speed = `set_execution_speed(speed)`
Limita la velocidad a la que se ejecuta el programa para ver mejor lo que está sucediendo.

Una `speed` de `1` es la velocidad que tiene el dron sin mejoras de velocidad.
Una `speed` de `10` hace que el código se ejecute `10` veces más rápido y corresponde a la velocidad del dron después de `9` mejoras de velocidad.
Una `speed` de `0.5` hace que el código se ejecute a la mitad de la velocidad sin mejoras de velocidad. Esto puede ser útil para ver lo que el código está haciendo.

Si `speed` es más rápida de lo que la ejecución puede ir actualmente, simplemente irá a la velocidad máxima.

Si `speed` es `0` o negativo, la velocidad se cambia de vuelta a la velocidad máxima.
El efecto también se detendrá cuando la ejecución se detenga.

devuelve `None`

toma `200` ticks para ejecutarse.

ejemplo de uso:
`set_execution_speed(1)`
@code_tooltip_set_world_size = `set_world_size(size)`
Limita el tamaño de la granja para ver mejor lo que está sucediendo.
También limpia la granja y restablece la posición del dron.
Configura la granja a una cuadrícula de `size` x `size`.
El `size` más pequeño posible es `3`.
Un `size` menor que `3` cambiará la cuadrícula de vuelta a su tamaño completo.
El efecto también se detendrá cuando la ejecución se detenga.

devuelve `None`

toma `200` ticks para ejecutarse.

ejemplo de uso:
`set_world_size(5)`
@code_tooltip_num_items = `num_items(item)`
Descubre cuánto de `item` tienes actualmente.

devuelve el número de `item` que tienes actualmente en tu inventario.

toma `1` tick para ejecutarse.

ejemplo de uso:
`if num_items(Items.Fertilizer) > 0:
    use_item(Items.Fertilizer)`
@code_tooltip_get_cost = `get_cost(thing)`
Obtiene el costo de un `thing`.

Si `thing` es una entidad, obtiene la semilla necesaria para plantarla.
Si `thing` es un desbloqueo, obtiene el costo para desbloquearlo.

devuelve un diccionario con artículos como claves y números como valores. Cada artículo se mapea a cuánto se necesita.
devuelve `None` cuando se usa en un desbloqueo actualizable que ya está en el nivel máximo.

toma `1` tick para ejecutarse.

ejemplo de uso:
`cost = get_cost(Unlocks.Carrots)
for item in cost:
    if num_items(item) < cost[item]:
        print("no hay suficientes artículos para desbloquear zanahorias")`
@code_tooltip_clear = `clear()`
Remueve todo de la granja, mueve el dron de vuelta a la posición `(0,0)` y cambia el sombrero de vuelta al predeterminado.

devuelve `None`

toma `200` ticks para ejecutarse.

ejemplo de uso:
`clear()`
@code_tooltip_get_companion = `get_companion()`
Obtiene la preferencia de compañero de la entidad debajo del dron.

devuelve una tupla en la forma `(companion_type, (companion_x_position, companion_y_position))`

toma `1` tick para ejecutarse.

ejemplo de uso:
`companion = get_companion()
if companion != None:
	print(companion)`
@code_tooltip_unlock = `unlock(unlock)`
Tiene exactamente el mismo efecto que hacer clic en el botón correspondiente a `unlock` en el árbol de investigación.

devuelve `True` si el desbloqueo fue exitoso, `False` de lo contrario.

toma `200` ticks para ejecutarse si tuvo éxito, `1` tick de lo contrario.

ejemplo de uso:
`unlock(Unlocks.Carrots)`
@code_tooltip_num_unlocked = `num_unlocked(thing)`
Usado para verificar si un desbloqueo, entidad, suelo o artículo ya está desbloqueado.

devuelve `1` más el número de veces que `thing` ha sido mejorado si `thing` es mejorable. De lo contrario, devuelve `1` si `thing` está desbloqueado, `0` de lo contrario.

toma `1` tick para ejecutarse.

ejemplo de uso:
`plant(Entities.Bush)
n_substance = get_world_size() * num_unlocked(Unlocks.Mazes)
use_item(Items.Weird_Substance, n_substance)`
@code_tooltip_reset = `reset()`
Restaura la granja a un cuadrado de 1x1, elimina todos los recursos y bloquea la mayoría de los desbloqueos.
No elimina tu código.

devuelve `None`

toma `200` ticks para ejecutarse.

ejemplo de uso:
`reset()`
@code_tooltip_measure = `measure()`
Puede medir algunos valores en algunas entidades. El efecto de esto depende de la entidad.

sobrecargas:
`measure()` mide la entidad debajo del dron.
`measure(direction)` mide la entidad vecina en la `dirección` del dron.

devuelve el número de pétalos de un girasol.
devuelve la próxima posición para un tesoro o manzana.
devuelve el tamaño de un cactus.
devuelve un número misterioso para una calabaza.
devuelve `None` para todas las demás entidades.

toma `1` tick para ejecutarse.

ejemplo de uso:
`num_petals = measure()`
@code_tooltip_leaderboard_run = `leaderboard_run(leaderboard, file_name, speedup)`
Inicia una simulación cronometrada para el `leaderboard` usando el `file_name` especificado como punto de partida.
`speedup` establece el aumento de velocidad inicial.

devuelve `None`

toma `200` ticks para ejecutarse.

ejemplo de uso:
`leaderboard_run(Leaderboards.Fastest_Reset, "full_run", 256)`
@code_tooltip_simulate = `simulate(filename, sim_unlocks, sim_items, sim_globals, seed, speedup)`
Inicia una simulación para el leaderboard usando el `file_name` especificado como punto de partida.

`sim_unlocks`: Una secuencia que contiene los desbloqueos iniciales.
`sim_items`: Un diccionario que mapea artículos a cantidades. La simulación comienza con estos artículos.
`sim_globals`: Un diccionario que mapea nombres de variables a valores. La simulación comienza con estas variables en el ámbito global.
`seed`: La semilla aleatoria de la simulación. Debe ser un entero positivo.
`speedup`: El aumento de velocidad inicial.

devuelve el tiempo que tomó ejecutar la simulación.

toma `200` ticks para ejecutarse.

ejemplo de uso:
`filename = "f1"
sim_unlocks = Unlocks
sim_items = {Items.Carrot : 10000, Items.Hay : 50}
sim_globals = {"a" : 13}
seed = 0
speedup = 64

run_time = simulate(filename, sim_unlocks, sim_items, sim_globals, seed, speedup)`
@code_tooltip_quick_print = `quick_print()`
Imprime un valor igual que `print()`, pero no se detiene para escribirlo en el aire, por lo que solo puede encontrarse en la página de salida.

devuelve `None`

toma `0` ticks para ejecutarse.

ejemplo de uso:
`quick_print("hi mom")`
@code_tooltip_change_hat = `change_hat(hat)`
Cambia el sombrero del dron a `hat`.

devuelve `None`

toma `200` ticks para ejecutarse.

ejemplo de uso:
`change_hat(Hats.Dinosaur_Hat)`

@code_tooltip_max = `max(a,b)`
Obtiene el máximo de una secuencia de elementos o varios argumentos pasados.
Puede ser usado con números y cadenas.

sobrecargas:
`max(a,b,c)`: Devuelve el máximo de los argumentos pasados.
`max(sequence)`: Devuelve el máximo de todos los valores en una secuencia.

toma #comparisons ticks para ejecutarse.

ejemplo de uso:
`max([3,6,34,16])`
@code_tooltip_min = `min(a,b)`
Obtiene el mínimo de una secuencia de elementos o varios argumentos pasados.
Puede ser usado con números y cadenas.

sobrecargas:
`min(a,b,c)`: Devuelve el mínimo de los argumentos pasados.
`min(sequence)`: Devuelve el mínimo de todos los valores en una secuencia.

el tiempo de ejecución depende de la entrada.

ejemplo de uso:
`min([3,6,34,16])`
@code_tooltip_abs = `abs(number)`
Calcula el valor absoluto de un `number`.

devuelve `number` si `number` es positivo, `-number` de lo contrario.

toma #comparisons ticks para ejecutarse.

ejemplo de uso:
`abs(-69)`
@code_tooltip_random = `random()`
Muestra un número aleatorio entre 0 (inclusive) y 1 (exclusivo).

devuelve el número aleatorio.

toma `1` tick para ejecutarse.

ejemplo de uso:
`def random_elem(list):
	index = random() * len(list) // 1
	return list[index]`
@code_tooltip_append = `list.append(element)`
Añade `element` al final de la `list`.

devuelve `None`

toma `1` tick para ejecutarse.

ejemplo de uso:
`list = []
list.append(1)`
@code_tooltip_add = `set.add(element)`
Añade `element` al `set`.

devuelve `None`

toma `element size` ticks para ejecutarse.

ejemplo de uso:
`set = {0}
set.add(1)`
@code_tooltip_remove = `collection.remove(element)`
Remueve la primera ocurrencia de `element` de la `collection`.

devuelve `None`

toma `element size` ticks para ejecutarse en un conjunto, #comparisions + #shifts ticks en una lista.

ejemplo de uso:
`list = [True, False, None]
list.remove(False)`
@code_tooltip_pop = `collection.pop()`
Remueve el último elemento de una lista o el elemento especificado de un diccionario.
`list.pop(i)` remueve el elemento en el índice `i` de la `list`.

devuelve el elemento removido

toma `key size` ticks para ejecutarse en un diccionario, `len(list) - i + 1` ticks en una lista.

ejemplo de uso:
`list = [True, False, None]
list.pop(0)`
@code_tooltip_insert = `list.insert(i, element)`
Inserta `element` en la `list` en el índice `i`.

devuelve `None`

toma `len(list) - i + 1` ticks.

ejemplo de uso:
`list = [1,2]
list.insert(0, 0)`
@code_tooltip_Items = Contiene todos los artículos que pueden estar en el inventario. Puede iterarse con un bucle `for`.
@code_tooltip_Entities = Contiene todas las plantas y otras entidades que pueden estar en un tile de suelo de la granja. Puede iterarse con un bucle `for`.
@code_tooltip_Grounds = Contiene todos los tipos de suelo posibles. Puede iterarse con un bucle `for`.
@code_tooltip_Unlocks = Contiene todos los desbloqueos y mejoras en el menú de investigación. Puede iterarse con un bucle `for`.
@code_tooltip_Hats = Contiene todos los tipos de sombreros. Puede iterarse con un bucle `for`.
@code_tooltip_Leaderboards = Contiene todas las categorías de tablas de clasificación. Puede iterarse con un bucle `for`.
@code_tooltip_for = Un bucle que itera sobre todos los elementos de una secuencia. Algunos lenguajes lo llaman un bucle "foreach".
@code_tooltip_while = Bucle hasta que la condición sea falsa.
@code_tooltip_def = Define una función.
@code_tooltip_True = Un valor booleano que siempre es verdadero.
@code_tooltip_False = Un valor booleano que siempre es falso.
@code_tooltip_if = Ejecuta código si la condición es `True`.
@code_tooltip_else = Ejecuta código si la condición anterior de `if` es `False`.
@code_tooltip_elif = Hace lo mismo que:
`else:
    if condition:`
@code_tooltip_None = Un valor que representa que no hay valor.
@code_tooltip_continue = Continúa inmediatamente con la siguiente iteración del bucle. Si hay bucles anidados, esto siempre afectará al bucle más interno.
@code_tooltip_break = Sale de un bucle y continúa ejecutando las instrucciones después del bucle. Si hay bucles anidados, esto siempre afectará al bucle más interno.
@code_tooltip_North = La dirección hacia arriba en la pantalla. A menos que gires tu pantalla.
@code_tooltip_East = La dirección hacia la derecha en la pantalla. A menos que gires tu pantalla.
@code_tooltip_South = La dirección hacia abajo en la pantalla. A menos que gires tu pantalla.
@code_tooltip_West = La dirección hacia la izquierda en la pantalla. A menos que gires tu pantalla.
@code_tooltip_not = `not True` es `False` y `not False` es `True`.
@code_tooltip_and = Evalúa el primer operando. Si es falso (`False`, `0` y colecciones vacías), devuelve ese valor inmediatamente (evaluación de cortocircuito), de lo contrario, evalúa y devuelve el segundo operando.
@code_tooltip_or = Evalúa el primer operando. Si es verdadero (cualquier cosa excepto `False`, `0` y colecciones vacías), devuelve ese valor inmediatamente (evaluación de cortocircuito), de lo contrario, evalúa y devuelve el segundo operando.
@code_tooltip_return = Se usa para devolver un valor desde una función.
@code_tooltip_pass = No hace nada. Puede ser útil porque no se permiten bloques de código vacíos.
